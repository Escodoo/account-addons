<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2024 - TODAY, Kaynnan Lemes <kaynnan.lemes@escodoo.com.br>
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>

    <!-- Compensate Advance -->
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form in (account_compensate_advance)</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='move_type']" position="after">
                <field name="advance_invoice" invisible="1" />
            </xpath>
            <xpath expr="//button[@name='action_register_payment']" position="after">
                <button
                    name="action_compensate_advance"
                    id="account_advance_compensate_btn"
                    type="object"
                    class="oe_highlight"
                    attrs="{'invisible': ['|', '|', '|', ('state', '!=', 'posted'), ('payment_state', 'not in', ('not_paid', 'partial')), ('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt')), ('advance_invoice', '=', False)]}"
                    string="Compensate Advance"
                    groups="account.group_account_invoice"
                />
            </xpath>
            <xpath expr="//sheet" position="before">
                <div
                    groups="account.group_account_invoice,account.group_account_readonly"
                    class="alert alert-info"
                    role="alert"
                    style="margin-bottom:0px;"
                    attrs="{'invisible': ['|', '|', '|', ('state', '!=', 'posted'), ('payment_state', 'not in', ('not_paid', 'partial')), ('move_type', 'not in', ('out_invoice', 'out_refund', 'out_receipt', 'in_receipt')), ('advance_invoice', '=', False)]}"
                >
                    You have advances that can be compensate for this customer using the button <strong
                    >Compensate Advance.</strong>
                </div>
                <div
                    groups="account.group_account_invoice,account.group_account_readonly"
                    class="alert alert-info"
                    role="alert"
                    style="margin-bottom:0px;"
                    attrs="{'invisible': ['|', '|', '|', ('state', '!=', 'posted'), ('payment_state', 'not in', ('not_paid', 'partial')), ('move_type', 'not in', ('in_invoice', 'in_refund', 'out_receipt', 'in_receipt')), ('advance_invoice', '=', False)]}"
                >
                    You have advances that can be compensate for this vendor using the button <strong
                    >Compensate Advance.</strong>
                </div>
            </xpath>
        </field>
    </record>
    <!-- Invoice Filter -->
    <record id="view_account_invoice_filter" model="ir.ui.view">
        <field
            name="name"
        >account.invoice.filter in (account_compensate_advance)</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_account_invoice_filter" />
        <field name="arch" type="xml">
            <xpath expr="//filter[@name='to_check']" position="after">
                <separator />
                <filter
                    string="Advance"
                    name="account_advance"
                    domain="[('line_ids.account_id.user_type_id','=',%(account.data_account_type_prepayments)d)]"
                />
            </xpath>
        </field>
    </record>

</odoo>
