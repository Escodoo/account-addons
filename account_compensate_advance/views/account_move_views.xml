<?xml version="1.0" encoding="utf-8" ?>
<!-- Copyright 2024 - TODAY, Kaynnan Lemes <kaynnan.lemes@escodoo.com.br>
     License AGPL-3.0 or later (https://www.gnu.org/licenses/agpl). -->
<odoo>

    <!-- Compensate Advance -->
    <record id="view_move_form" model="ir.ui.view">
        <field name="name">account.move.form in (account_compensate_advance)</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form" />
        <field name="arch" type="xml">
            <xpath expr="//button[@name='action_register_payment']" position="after">
                <button
                    name="action_compensate_advance"
                    id="account_advance_compensate_btn"
                    type="object"
                    class="oe_highlight"
                    attrs="{'invisible': ['|', '|', ('state', '!=', 'posted'), ('payment_state', 'not in', ('not_paid', 'partial')), ('move_type', 'not in', ('out_invoice', 'out_refund', 'in_invoice', 'in_refund', 'out_receipt', 'in_receipt'))]}"
                    string="Compensate Advance"
                    groups="account.group_account_invoice"
                />
            </xpath>
            <xpath expr="//field[@name='id']" position="after">
                <field name="is_advance_move" invisible="1" />
            </xpath>
        </field>
    </record>

    <!-- Advance -->
    <record id="action_advance_create_in_invoice" model="ir.actions.act_window">
        <field name="name">Create Advance - Supplier</field>
        <field name="res_model">account.create.advance.journal</field>
        <field name="view_mode">form</field>
        <field name="context">{'move_type': 'in_invoice'}</field>
        <field name="target">new</field>
    </record>

    <record id="action_advance_create_out_invoice" model="ir.actions.act_window">
        <field name="name">Create Advance - Customer</field>
        <field name="res_model">account.create.advance.journal</field>
        <field name="view_mode">form</field>
        <field name="context">{'move_type': 'out_invoice'}</field>
        <field name="target">new</field>
    </record>

    <record id="action_advance_out_invoice_type" model="ir.actions.act_window">
        <field name="name">Advance - Customer</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="view_id" ref="account.view_move_tree" />
        <field name="search_view_id" ref="account.view_account_invoice_filter" />
        <field name="domain">[('journal_id.is_advance_journal', '=', True),
            ('journal_id.advance_account_type', '=', 'customer')]
        </field>
        <field
            name="context"
        >{'default_move_type': 'entry', 'search_default_misc_filter':1,
            'view_no_maturity': True}</field>
    </record>

    <record id="action_advance_in_invoice_type" model="ir.actions.act_window">
        <field name="name">Advance - Supplier</field>
        <field name="res_model">account.move</field>
        <field name="view_mode">tree,kanban,form</field>
        <field name="view_id" ref="account.view_move_tree" />
        <field name="search_view_id" ref="account.view_account_invoice_filter" />
        <field name="domain">[('journal_id.is_advance_journal', '=', True),
            ('journal_id.advance_account_type', '=', 'supplier')]</field>
        <field
            name="context"
        >{'default_move_type': 'entry', 'search_default_misc_filter':1,
            'view_no_maturity': True}</field>
    </record>

</odoo>
